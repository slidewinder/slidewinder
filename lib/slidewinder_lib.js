// Generated by CoffeeScript 1.10.0
(function() {
  var PresentationFramework, _, fm, fs, handlebars, load_collection, log, log_colours, logger, mkdirp, path, pick_slides, slidewinder, yaml;

  fs = require('fs');

  path = require('path');

  fm = require('front-matter');

  handlebars = require('handlebars');

  _ = require('lodash');

  mkdirp = require('mkdirp');

  logger = require('./log.js');

  yaml = require('js-yaml');

  log_colours = {
    silly: 'magenta',
    input: 'grey',
    verbose: 'cyan',
    prompt: 'grey',
    debug: 'blue',
    info: 'green',
    data: 'grey',
    help: 'cyan',
    warn: 'yellow',
    error: 'red'
  };

  load_collection = function(dirpath) {
    var collection;
    collection = {};
    fs.readdirSync(dirpath).forEach(function(file) {
      var data, filepath, name, slide;
      filepath = path.resolve(dirpath, file);
      data = fs.readFileSync(filepath, 'utf8');
      slide = fm(data);
      name = slide.attributes.name;
      if (collection[name]) {
        return log.error('Multiple slides have the name', name);
      } else {
        return collection[name] = slide;
      }
    });
    log.info('Loaded', Object.keys(collection).length, 'markdown slide files from', dirpath);
    return collection;
  };

  pick_slides = function(collection, selections) {
    var picked;
    picked = [];
    selections.forEach(function(selection) {
      var slide;
      slide = collection[selection];
      if (slide) {
        return picked.push(slide);
      } else {
        return log.error('No slide found with name', name);
      }
    });
    log.info('Picked', picked.length, 'slides from collection');
    return picked;
  };

  PresentationFramework = function(framework) {
    var fw, template;
    log.info('Looking for presentation framework module: ', framework);
    fw = this;
    template = fs.readFileSync(path.join(framework, 'template.html'), 'utf8');
    fw.helpers = require(path.join(framework, 'helpers.js'));
    fw.render_deck = function(data) {
      return fw.renderer(data);
    };
    return fw;
  };

  PresentationFramework.registerHelpers = function() {
    var fw;
    fw = this;
    return Object.keys(fw.helpers).forEach(function(key) {
      return handlebars.registerHelper(key, fw.helpers[key]);
    });
  };

  slidewinder = function(sessiondata) {
    var allslides, deck, plugin;
    allslides = load_collection(sessiondata.collection);
    sessiondata.slideset = pick_slides(allslides, sessiondata.slides);
    plugin = PresentationFramework(sessiondata.framework);
    console.log(plugin);
    process.exit();
    deck = plugin.render_deck(sessiondata);
    return save_deck(deck, data);
  };

  log = logger();

  module.exports = slidewinder;

}).call(this);
