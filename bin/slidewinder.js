// Generated by CoffeeScript 1.10.0
(function() {
  var fwpath, list, path, pjson, program, slidewinder;

  program = require('commander');

  path = require('path');

  slidewinder = require('../lib/slidewinder_lib.js');

  pjson = require('../package.json');

  list = function(val) {
    return val.split(',');
  };

  fwpath = function(framework) {
    return path.resolve(__dirname, "../extensions/frameworks", framework);
  };

  program.version(pjson.version).option('-s, --slides <slide list>', 'Comma-separated list of slides to use (no spaces)', list).option('-c, --collection <path>', 'Path to slide collection').option('-o, --output <path>', 'Path to save output (will create directory)').option('-t, --title <title>', 'Title of the deck').option('-a, --author <name>', 'Deck author').option('-f, --framework <framework>', 'HTML and JS Framework or plugin to use for slideshow generation.', fwpath).parse(process.argv);

  if (!process.argv.slice(2).length) {
    program.help();
  }

  if (program.framework == null) {
    program.framework = fwpath('remark');
  }

  slidewinder(program);

}).call(this);
